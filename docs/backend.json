
{
  "entities": {
    "user": {
      "title": "User",
      "description": "A user of the application.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "fullName": {
          "type": "string",
          "description": "The user's full name."
        },
        "role": {
          "type": "string",
          "description": "The user's role in the party.",
          "enum": [
            "President",
            "Chairman",
            "Vice Chairman",
            "VP",
            "Sec Gen",
            "Treasurer",
            "Auditor",
            "VP Ways & Means Chair",
            "VP Media Comms",
            "VP Soc Med Comms",
            "VP Events and Programs",
            "VP Membership",
            "VP legal affairs",
            "Member",
            "Admin",
            "System Admin"
          ]
        },
        "jurisdictionLevel": {
          "type": "string",
          "description": "The user's jurisdiction level.",
          "enum": [
            "National",
            "Regional",
            "Provincial",
            "City/Municipal",
            "Barangay"
          ]
        },
        "assignedLocation": {
          "type": "string",
          "description": "The name of the user's assigned location (e.g., province, city)."
        },
        "photoURL": {
          "type": "string",
          "description": "The URL or data URI of the user's profile picture."
        },
        "resumeURL": {
          "type": "string",
          "description": "The URL of the user's uploaded resume file."
        },
        "resumeText": {
          "type": "string",
          "description": "The text-based content of the user's resume."
        },
        "kartilyaAgreed": {
          "type": "boolean",
          "description": "Whether the user has agreed to the Kartilya."
        },
        "passwordIsTemporary": {
          "type": "boolean",
          "description": "Whether the user needs to change their temporary password."
        },
        "isApproved": {
          "type": "boolean",
          "description": "Whether the user is approved."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user document was created."
        }
      },
      "required": ["uid", "email", "fullName", "role", "jurisdictionLevel", "kartilyaAgreed"]
    }
  },
  "auth": {
    "providers": ["email-password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/user" },
      "description": "Stores user profile information."
    }
  }
}
